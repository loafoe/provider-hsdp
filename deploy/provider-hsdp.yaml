apiVersion: apps/v1
kind: Deployment
metadata:
  name: provider-hsdp
  namespace: crossplane-system
  labels:
    app: provider-hsdp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: provider-hsdp
  template:
    metadata:
      labels:
        app: provider-hsdp
    spec:
      serviceAccountName: provider-hsdp
      containers:
      - name: provider
        image: ghcr.io/loafoe/provider-hsdp:dev
        imagePullPolicy: Always
        args:
        - --debug
        ports:
        - containerPort: 8080
          name: metrics
        resources:
          limits:
            memory: 256Mi
          requests:
            cpu: 100m
            memory: 128Mi
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: provider-hsdp
  namespace: crossplane-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: provider-hsdp
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: provider-hsdp
  namespace: crossplane-system
